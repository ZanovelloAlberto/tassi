

// Check if there are at least two command-line arguments (including the script name)
// if (Deno.args.length < 2) {
//     console.error("Usage: deno run hello.ts <arg1> <arg2> ...");
//     Deno.exit(1);
//   }
  
//   // Extract the command-line arguments, excluding the first one (the script name)
//   const args = Deno.args.slice(1);
  
//   // Print "Hello, World" and the provided arguments
//   console.log("Hello, World", ...args);